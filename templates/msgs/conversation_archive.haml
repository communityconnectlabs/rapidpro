-extends 'includes/modal.html'
-load i18n temba

-block modal
  {{block.super}}
    -trans "You are about to"
    {{submit_button_name|lower}}
    %span.font-normal
      {{object}}.
    -trans "Are you sure?"

    .hidden
      %form(method="POST")
        %input(type="submit" value="{{submit_button_name}}")